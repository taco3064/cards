import styled from 'styled-components';

export default {
  Nav: styled.nav`
    display: flex;
    justify-content: center;
    gap: 8px;
    padding: 8px 12px;
    margin-top: auto;
    min-height: 48px;
  `,
};
